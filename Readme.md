# Schildcafe GUI with GPT-5.1-Codex-Max via Codex

This version of the application was generated by codes (`npm i -g @openai/codex`) using GPT-5.1-Codex-Max.
## Prompt

The following prompt was given:

> Make a beautiful single page application "Schildcafe" without the use of any framework.
> The application is a frontend to the Servitor defined in the `swagger.json` OpenAPI specification.
> Add functionality to submit new orders with multiple products.
> Add functionality to follow the order's status and to retrieve it once finished.
> Use the logo in `assets/logo.png` as the logo of the application.
> The API base URL and the list of available items should be loaded from a `config.json`.

## Iterations

Codex ran, following the instructions from Agents.md and maintained the Changelog throughout and comitted.

After inital run the config wasn't loading. Several prompts:

> the config.json isn't loading

> the config.json is available, but the code is stuck at "Awaiting configuration…"

> it is still all disabled. the server is running. can you check?

> From the browser's console: Uncaught SyntaxError: expected expression, got ',' at localhost:8000:374:59

Then it finally worked, but

> The order shows up in the "Kitchen board" but not in the "Follow & Retrieve" which is stuck on "Order not visible yet. Try again shortly."

Fix had no effect, run ended with these stats

```shell
Token usage: total=118,766 input=95,466 (+ 880,896 cached) output=23,300 (reasoning 10,496)
```

## Result

This is what it looks like with the order in the "Kitchen board", but not retrievable.

![Screenshot](screenshot.png "Screenshot")
